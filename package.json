{
  "name": "veracity-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss" : "node-sass --watch src/scss -o build/styles",
    "serve":"live-server",
    "dev" : "npm run scss & npm run serve"
  },
  "author": "veracity",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2",
    "node-sass": "^8.0.0"
  }
}
